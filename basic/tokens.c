//
// Created by Pras Adi on 06/10/22.
//

int main() {

    /*
     * This is an example of variable declare in C
     * int, double are keywords
     * amount, total balance are keywords
     */
    int amount;
    double totalbalance;

    /*
     * You can also declare a variable like this
     * There are 32 reserved keywords in C
     * auto, break, case, char, const, continue,
     * default, do, double, else, enum, extern, float,
     * for, goto, if, int, long, register, return,
     * short, signed, sizeof, static, struct, switch,
     * typedef, union, unsigned, void, volatile, while.
     * !! Don't use keyword as identifier !!
     */
    float a, b;

    return 0;
}
